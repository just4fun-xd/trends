# ROADMAP 2026-07k — лаборатории 37+D.1, таймфреймы, дедуп, баг-фиксы

Продолжает 2026-07j. Сессия С боевыми прогонами (данные Кирилла
локально, результаты вложены в чат). Реестр 132 -> 170.

## СДЕЛАНО в 2026-07k

### Новые стратегии (37 + D.1), все протестированы bootstrap
- `trend_lab4.py` (10) — PSAR, Renko, Mann-Kendall, AR(1)-гейт,
  Шмитт, декиклер Элерса, overnight, CUSUM Пейджа, Келли, mom-pct.
- `crypto_aggr_lab2.py` (10) — CPPI, Grossman-Zhou, BNS-jump, CVaR,
  полудисперсия, vol-of-vol, Хоукс, skew, Келли-t, circuit-breaker.
- `meanrev_lab3.py` (10) — Бертрам, GARCH-шок, Келли-MR, грид,
  овершут, DFA, ранговый шок, хвост-квантиль, AR(1)-прогноз, дивер.
- `mr_lowvol2.py` (5) — доработка чемпиона по одному узлу.
- `schwartz_smith.py` (2, D.1) — Kalman ln(S)=chi+xi.

### Вердикты (bootstrap, два источника где возможно)
- **Champion-стек НЕПОКОЛЕБИМ** на сырье/крипте: все 37 -> TIE/WORSE.
- Рейтинг наврал дважды (mr_lv2_zexit 1.51, tr4_mk 1.27) — bootstrap
  дал TIE с разными знаками на источниках = миражи. Протокол сработал.
- **Приобретения**: ca2_cppi (крипто-оборона, corr 0.5, ens_ok),
  mr_connors (equity-MR ансамбль, corr 0.31, combo 1.33).

### D.1 Schwartz-Smith — ЗАКРЫТ С МЕХАНИЗМОМ
ss_chi_soft 0.09, ss_chi_mr -0.03 vs mr_lowvol 1.35. Причина
структурная: недоидентификация chi на ОДНОМ roll-adj ряду (нет term
structure, rho=0). OU-как-двигатель закрыт по ВСЕМ формам. OU-как-
измеритель жив (kappa->пороги mr3_bertram, half-life->окна).
Последний шанс — панель РЕАЛЬНЫХ фьючерсных сроков (другой pipeline,
низкий приор). Рекомендация: не воскрешать.

### Дедупликация
- tr3_tsmom = tsmom_multi -> унифицирован в параметризованный движок
  + per-asset tsmom_eq(63,126,252)/cr1d(21,63,252)/cr4h(42,126,504)/
  comm(21,63,252).
- tr3_supertrend удалён (= tr_supertrend).
- tr3_kama_slope удалён (= kama_trend + NaN-вирус prev=c[0]).
- tr3_zlema ОСТАВЛЕН, помечен (ниша ZLEMA с tr_zlema).

### БАГ-ФИКСЫ (критично — влияют на прошлые числа)
1. **tsmom_multi**: (pct_change>0).astype давал ЛОЖНЫЙ 0 на
   NaN-прогреве/дырах склейки -> систематический шорт на стыках
   контрактов. Теперь NaN->NaN (движок пропускает). ВСЕ tsmom-прогоны
   сессии шли со старым багом; на непрерывных yf влияние мало, на
   databento-склейке числа могли быть занижены. Перемерить sweep.
2. **kama_trend** (ЖИВОЙ чемпион-семейства): NaN-вирус prepend=
   close[0] + старт started на NaN-close -> вечный 0 на склейке.
   Исправлено (первый валидный close, старт только на не-NaN).
3. **yfinance h1/h4**: нормализация -> 1h/4h (падало 422).

### Двойной лаг — проверено, НЕ баг
channel_pos/chandelier/adx_donchian: .shift(1) на КАНАЛЕ — часть
сигнала (вчерашний уровень), не лаг позиции. Эмпирически: лаг ровно 1
(движок), не 2. Комментарий 07j про «двойной shift» относился к
.shift на ИТОГОВОЙ позиции — другое место.

### Таймфреймы (гипотеза Кирилла — проверена)
- **1h ДЕГРАДИРУЕТ всё**: MR-сырьё -0.07, крипто-тренд 0.79. Механизм:
  микрошум bid-ask + издержки часового оборота. Гипотеза «1h для MR»
  ОПРОВЕРГНУТА с механизмом.
- **4h оптимален** для крипто/сырья-тренда: сырьё 4h 1.25 vs 1d 0.60
  (!). Нейтрален для MR (4h≈1d). Таймфрейм важнее горизонтов.
- **1d правильный** для акций и heavy-крипты. Акции 4h деградируют
  тренд (1.19->0.79) — медленные макро-тренды тонут в 4h-шуме.
- Пересмотр вывода: акциям НЕ нужны длинные горизонты — в едином окне
  короткий (21,63,252) не хуже длинного (63,126,252).

### Инфраструктура / данные
- Databento intraday настроен: XNAS.ITCH (акции, с 2018, ~$0),
  GLBX.MDP3 1h (сырьё). check_databento_access.py (get_cost до траты).
- 4h-панели сырья (2021) и акций (2018) выгружены, проверены
  check_4h_panels.py. 1h-сырьё выгружено.
- run_contribution_sweep.py (карта рабочих пар), авто-путь панели,
  полные имена активов, LOO зона-шума + corr-колонка.
- Тесты: test_labs_2026_07k.py (37 + NaN-регрессия). 127 passed.

## НА ГОРИЗОНТЕ (2026-07l)

1. **Перемерить tsmom-sweep с NaN-фиксом** — числа склейки могли
   сдвинуться. Единое окно 2021, все классы.
2. **Балласт-вырезы через bootstrap** (не только LOO): соя ZS/ZL/ZM
   (сырьё-4h), Wheat/Corn, Litecoin (крипта), MA/PEP (акции). LOO
   говорит «возможно», bootstrap — «значимо ли». Второй источник.
3. **tr3_tsmom watch equity** — BEATS на грани значимости (corr 0.9,
   одна история). Не менять ema_vt до 2-го режима/куска истории.
4. **ca2_cppi в крипто-ансамбль** — проверить как 4-й leg (corr 0.5).
5. **mr_connors в equity-MR** — combo 1.33, но абсолют тонкий (+2.2%
   годовых); подать как диверсификатор, не доходность.
6. **Отчёт Александру** — .docx EN, вся сессия (черновик
   tsmom_multi_report_EN.md готов).

## ЗАКРЫТО ОКОНЧАТЕЛЬНО (не переоткрывать)
- OU-как-двигатель: все формы, включая Schwartz-Smith (D.1).
- Equity short leg, carry, seasonal, pairs — с прошлых сессий.
- 1h-таймфрейм для тренда и MR (микрошум+издержки).
- Качество-фильтры тренда (universal weak: trend_lab2/impulse на
  сырье И акциях WORSE).
